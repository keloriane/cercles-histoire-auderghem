import styled from 'styled-components';

export const Input_Wrapper = styled.span`
  font-style: normal;
  font-weight: normal;
  font-size: 20px;
  line-height: 22px;
  letter-spacing: 0.1em;
  text-transform: capitalize;
  color: #000000;
  padding: 10px;
`;

export const Input_Radio = styled.input`
  position: relative;
`;
