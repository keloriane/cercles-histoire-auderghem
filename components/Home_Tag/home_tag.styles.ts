import styled from 'styled-components';

export const Tag_Link = styled.a`
  font-style: normal;
  font-weight: 300;
  font-size: 29.44px;
  line-height: 38px;
  letter-spacing: 0.1em;
  text-transform: uppercase;
  position: relative;
  z-index: 2;

  color: rgba(255, 255, 255, 0.2);
  &:hover {
    color: #ffffff;
  }
`;
